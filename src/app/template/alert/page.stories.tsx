import type { Meta, StoryObj } from "@storybook/react";
import AlertPage from "./page";

const meta = {
  title: "Pages/Alert",
  component: AlertPage,
} satisfies Meta<typeof AlertPage>;

export default meta;

export const Default: StoryObj = {};
