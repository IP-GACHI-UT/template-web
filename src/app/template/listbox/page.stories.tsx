import type { Meta, StoryObj } from "@storybook/react";
import ListboxPage from "./page";

const meta = {
  title: "Pages/Listbox",
  component: ListboxPage,
} satisfies Meta<typeof ListboxPage>;

export default meta;

export const Default: StoryObj = {};
