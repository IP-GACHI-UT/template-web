import type { Meta, StoryObj } from "@storybook/react";
import HeadingPage from "./page";

const meta = {
  title: "Pages/Heading",
  component: HeadingPage,
} satisfies Meta<typeof HeadingPage>;

export default meta;

export const Default: StoryObj = {};
