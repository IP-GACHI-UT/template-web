import type { Meta, StoryObj } from "@storybook/react";
import DescriptionListPage from "./page";

const meta = {
  title: "Pages/DescriptionList",
  component: DescriptionListPage,
} satisfies Meta<typeof DescriptionListPage>;

export default meta;

export const Default: StoryObj = {};
