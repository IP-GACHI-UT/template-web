import type { Meta, StoryObj } from "@storybook/react";
import AvatarPage from "./page";

const meta = {
  title: "Pages/Avatar",
  component: AvatarPage,
} satisfies Meta<typeof AvatarPage>;

export default meta;

export const Default: StoryObj = {};
