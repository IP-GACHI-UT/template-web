import type { Meta, StoryObj } from "@storybook/react";
import RadioPage from "./page";

const meta = {
  title: "Pages/Radio",
  component: RadioPage,
} satisfies Meta<typeof RadioPage>;

export default meta;

export const Default: StoryObj = {};
