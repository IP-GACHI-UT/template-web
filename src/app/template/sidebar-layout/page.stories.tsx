import type { Meta, StoryObj } from "@storybook/react";
import SidebarLayoutPage from "./page";

const meta = {
  title: "Pages/SidebarLayout",
  component: SidebarLayoutPage,
} satisfies Meta<typeof SidebarLayoutPage>;

export default meta;

export const Default: StoryObj = {};
