import type { Meta, StoryObj } from "@storybook/react";
import ComboboxPage from "./page";

const meta = {
  title: "Pages/Combobox",
  component: ComboboxPage,
} satisfies Meta<typeof ComboboxPage>;

export default meta;

export const Default: StoryObj = {};
