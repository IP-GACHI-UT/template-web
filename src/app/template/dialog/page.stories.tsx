import type { Meta, StoryObj } from "@storybook/react";
import DialogPage from "./page";

const meta = {
  title: "Pages/Dialog",
  component: DialogPage,
} satisfies Meta<typeof DialogPage>;

export default meta;

export const Default: StoryObj = {};
