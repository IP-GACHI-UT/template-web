import type { Meta, StoryObj } from "@storybook/react";
import PaginationPage from "./page";

const meta = {
  title: "Pages/Pagination",
  component: PaginationPage,
} satisfies Meta<typeof PaginationPage>;

export default meta;

export const Default: StoryObj = {};
