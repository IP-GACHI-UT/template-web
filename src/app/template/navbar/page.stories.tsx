import type { Meta, StoryObj } from "@storybook/react";
import NavbarPage from "./page";

const meta = {
  title: "Pages/Navbar",
  component: NavbarPage,
} satisfies Meta<typeof NavbarPage>;

export default meta;

export const Default: StoryObj = {};
