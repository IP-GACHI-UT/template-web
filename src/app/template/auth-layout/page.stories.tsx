import type { Meta, StoryObj } from "@storybook/react";
import AuthLayoutPage from "./page";

const meta = {
  title: "Pages/AuthLayout",
  component: AuthLayoutPage,
} satisfies Meta<typeof AuthLayoutPage>;

export default meta;

export const Default: StoryObj = {};
